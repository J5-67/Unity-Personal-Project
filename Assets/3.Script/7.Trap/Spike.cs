using UnityEngine;

namespace Trap
{
    // [유니] 가시는 딱히 특별한 기능 없이 TrapBase만 상속받으면 끝!
    // 너무 심플해서 뻘줌하지만, 나중에 "독 가시" 같은 걸로 확장할 수 있으니 클래스는 분리!
    public class Spike : TrapBase
    {
        // 뾰족함 추가? 일단은 비워둠! 🤠
    }
}
